import { saveServerData } from "../../structures/functions/saveData.js";
import Discord from "discord.js";
/**
 *
 * @param {typeof import("../../index.js").default} client
 * @param {Discord.Message} message
 */
export default async (client, message) => {
  //await saveServerData(client, message.guild.id);
};
